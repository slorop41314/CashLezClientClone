import React, { Component } from 'react';
import { View , Text} from 'react-native';

export default class Taxdll extends Component {
    state = {  }
    render() {
        return (
            <Text>Tax </Text>
        );
    }
}